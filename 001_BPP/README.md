# Bin Packing Problem (BPP)

The **Bin Packing Problem (BPP)** consists of packing a set of items—each with a given size—into the minimum number of bins of fixed capacity.

### Objective
Minimize the number of bins used.

### Constraints
- Each item must be assigned to exactly one bin  
- Sum of item sizes in any bin cannot exceed its capacity
